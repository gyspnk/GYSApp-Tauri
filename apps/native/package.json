{
  "name": "@gys/native",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('Tauri shell scaffold: native build requires platform toolchain')\"",
    "typecheck": "node -e \"console.log('Native bridge types are checked in platform CI')\"",
    "test": "node -e \"console.log('Native contract tests run with platform adapters')\"",
    "lint": "node -e \"console.log('Native shell lint runs in platform CI')\""
  }
}
