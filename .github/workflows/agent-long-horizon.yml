name: Agent runner probe

on:
  push:
    branches:
      - audit/parity-ui-tts-20260911

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner startup
        run: echo "agent runner available"
