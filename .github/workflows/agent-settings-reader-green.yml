name: Agent settings and reader GREEN

on:
  push:
    branches:
      - audit/parity-ui-tts-20260911
    paths:
      - ".github/workflows/agent-settings-reader-green.yml"

permissions:
  contents: write

jobs:
  green:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/parity-ui-tts-20260911
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 11.21.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply minimal production fixes
        run: python3 scripts/agent-settings-reader-fix.py
      - name: Format changed source
        run: pnpm exec prettier --write apps/web/src/faith.tsx apps/web/src/more.tsx apps/web/src/literature.tsx apps/web/e2e/settings-data-integrity.spec.ts apps/web/e2e/reading-direct-flow.spec.ts
      - name: Verify code gates
        run: |
          pnpm format:check
          pnpm lint
          pnpm typecheck
          pnpm test
          pnpm build
          pnpm verify:bundle
      - name: Install Chromium
        run: pnpm --filter @gys/web exec playwright install --with-deps chromium
      - name: Verify focused regressions
        run: pnpm --filter @gys/web exec playwright test e2e/settings-data-integrity.spec.ts e2e/reading-direct-flow.spec.ts --project=chromium --reporter=line
      - name: Commit verified production fixes
        run: |
          git config user.name "gysapp-agent"
          git config user.email "actions@users.noreply.github.com"
          git rm scripts/agent-settings-reader-fix.py .github/workflows/agent-settings-red.yml .github/workflows/agent-settings-reader-green.yml
          git add apps/web/src/faith.tsx apps/web/src/more.tsx apps/web/src/literature.tsx apps/web/e2e/settings-data-integrity.spec.ts apps/web/e2e/reading-direct-flow.spec.ts
          git commit -m "fix: preserve reading settings and close direct reader"
          git push origin HEAD:audit/parity-ui-tts-20260911
