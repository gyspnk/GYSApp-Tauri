name: Agent prepush plan

on:
  pull_request:

permissions:
  contents: read

jobs:
  prepush-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --test scripts/prepush-plan.test.mjs
