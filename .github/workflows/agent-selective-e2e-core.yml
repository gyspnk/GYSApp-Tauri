name: Agent selective E2E core

on:
  pull_request:

permissions:
  contents: read

jobs:
  selective-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --test scripts/test-e2e-selective-core.test.mjs
